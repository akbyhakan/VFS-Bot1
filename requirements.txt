# Dependency Pinning Strategy:
# - ==  : Strict pin for critical/stable packages
# - ~=  : Compatible release (allows patch updates within same minor)
# - >=,<: Range pin where specific range is needed
# Dependency lockfile (requirements.lock) is used by Dockerfile for reproducible builds.
# To regenerate after changing dependencies: make lock

playwright>=1.58.0,<2.0.0
fastapi==0.128.7
python-multipart~=0.0.22
uvicorn==0.40.0
python-dotenv==1.0.1
pyyaml==6.0.2
aiohttp~=3.13.3
asyncpg~=0.30.0
# Alembic migration support
alembic~=1.14.0
sqlalchemy[asyncio]~=2.0.37
tenacity==9.0.0
python-telegram-bot==21.10
aiosmtplib==3.0.2
httpx~=0.28.1
jinja2==3.1.5
websockets==14.2
pydantic==2.10.6
pydantic-settings~=2.7.1
email-validator~=2.2.0
prometheus-client~=0.21.1
nopecha==2.0.1
twocaptcha==0.0.1
anticaptchaofficial==1.0.69
SpeechRecognition==3.10.1
pydub==0.25.1
# Anti-detection - REQUIRED for bot functionality
curl-cffi~=0.7.4  # Required for Cloudflare TLS fingerprint bypass
numpy>=2.2.0  # Required for realistic Bézier curve mouse movements (Python 3.12 compatible)
pyjwt~=2.10.1
loguru~=0.7.3
passlib[bcrypt]~=1.7.4
Pillow~=11.1.0
slowapi~=0.1.9
cryptography~=46.0.5
pycryptodome~=3.21.0
cachetools~=5.5.1
google-generativeai~=0.8.4  # Optional: AI-powered selector repair
redis[hiredis]~=5.2.1  # Required for distributed rate limiting in multi-worker deployments
