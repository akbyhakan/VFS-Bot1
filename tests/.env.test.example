# Example Test Environment Configuration
# Copy this file to .env.test and customize the values

# Environment
ENV=testing

# API Secret Key (minimum 64 characters for security)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(48))"
TEST_API_SECRET_KEY=your-test-api-secret-key-at-least-64-characters-long-please

# Encryption Key (base64-encoded Fernet key)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-test-encryption-key-here

# VFS Encryption Key
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
VFS_ENCRYPTION_KEY=your-test-vfs-encryption-key-here

# API Key Salt
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY_SALT=your-test-api-key-salt-here

# Database (optional, defaults to test database for tests)
# DATABASE_URL=postgresql://vfs_bot:password@localhost:5432/vfs_bot_test

# Note: If this file is not present, tests will generate random keys automatically
# This file is useful for consistent test environments or CI/CD pipelines
